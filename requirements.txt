# Exception Ledger — project dependencies
#
# Part 1 (data/generate_dataset.py) is deliberately STDLIB-ONLY: the dataset
# must be regenerable in any clean Python 3.9+ environment with no installs.
# The packages below are required by later build-order steps (Architecture v2 §5).

rapidfuzz==3.9.7      # §2.3 fuzzy vendor-name similarity in src/matcher.py
PyYAML==6.0.2         # §2.5 src/rules/rules_v2026_04.yaml loading
anthropic==1.2.0      # §2.2 normalize_ai_assisted() Claude API call (output_config needs >=1.x)
pytest==8.3.3         # §5 tests/
